# ✏️ 简历编辑功能使用说明

## 🎯 两种编辑模式

### 模式 1: 预览模式（推荐）
**特点：**
- ✅ 所见即所得
- ✅ 点击字段直接编辑
- ✅ 实时预览效果
- ✅ 适合快速修改

**使用方法：**
1. 点击工具栏的 👁️ 图标（默认模式）
2. 点击任意文本字段
3. 直接输入修改
4. 按 Enter 或点击外部保存

### 模式 2: 源码模式（高级）
**特点：**
- ✅ 直接编辑 JSON
- ✅ 批量修改
- ✅ 适合高级用户
- ✅ 可以复制粘贴

**使用方法：**
1. 点击工具栏的 ✏️ 图标
2. 编辑 JSON 代码
3. 点击外部或失焦自动保存

---

## 📝 可编辑的字段

### 基本信息
- ✅ 姓名（点击标题编辑）
- ✅ 电话（点击编辑）
- ✅ 邮箱（点击编辑）
- ✅ 位置（点击编辑）

### 其他字段（即将支持）
- ⏳ 技能标签
- ⏳ 工作经历
- ⏳ 项目经验
- ⏳ 教育背景

---

## 🧪 测试步骤

### 测试预览模式编辑

1. **生成简历**
   ```
   输入："帮我生成一份前端工程师的简历"
   ```

2. **切换到预览模式**
   - 点击工具栏的 👁️ 图标（应该已经是默认状态）

3. **编辑姓名**
   - 点击右侧的"张三"或"请填写您的姓名"
   - 输入框会出现
   - 输入你的名字，如"李四"
   - 按 Enter 或点击外部

4. **编辑电话**
   - 点击电话号码
   - 修改为你的电话
   - 保存

5. **查看效果**
   - 修改后的内容立即显示
   - 数据已更新

---

### 测试源码模式编辑

1. **切换到源码模式**
   - 点击工具栏的 ✏️ 图标

2. **查看 JSON**
   ```json
   {
     "basicInfo": {
       "name": "张三",
       "phone": "13800138000",
       "email": "zhangsan@example.com",
       "location": "北京"
     },
     "skills": [...],
     ...
   }
   ```

3. **编辑 JSON**
   - 修改任意字段
   - 例如：将 "name" 改为 "王五"

4. **保存修改**
   - 点击文本框外部
   - 应该看到提示："源码已更新"

5. **切换回预览模式**
   - 点击 👁️ 图标
   - 查看修改是否生效

---

## 🎨 EditableField 组件特性

### 当前实现
```vue
<EditableField
  :value="resumeData.basicInfo?.name || '姓名'"
  :path="'basicInfo.name'"
  @change="handleFieldChange"
/>
```

### 组件行为
1. **默认状态**：显示文本
2. **点击**：变为输入框
3. **编辑**：实时输入
4. **保存**：
   - 按 Enter
   - 失去焦点
   - 点击外部

### 数据流
```
用户点击 → 显示输入框 → 用户编辑 → 触发 change 事件
  ↓
emit('field-change', path, value)
  ↓
父组件接收 → 更新 resumeData → 触发重新渲染
  ↓
EditableField 显示新值
```

---

## 🔧 技术实现

### 1. EditableField 组件
```typescript
// src/components/resume/EditableField.vue
interface Props {
  value: string;
  path: string;
  placeholder?: string;
}

const emit = defineEmits<{
  (e: 'change', path: string, value: string): void;
}>();
```

### 2. 父组件处理
```typescript
// ResumePreviewPanel.vue
const handleFieldChange = (path: string, value: string) => {
  emit('field-change', path, value);
};

// ResumeGeneratorNew.vue
const handleFieldChange = (path: string, value: any) => {
  if (!path) {
    // 源码模式：替换整个数据
    resumeData.value = value;
  } else {
    // 预览模式：更新单个字段
    _set(resumeData.value, path, value);
  }
  
  // 保存版本
  addVersion(resumeData.value, `手动修改 ${path}`, [{
    action: 'update',
    path,
    value
  }]);
};
```

---

## 📊 编辑模式对比

| 特性 | 预览模式 | 源码模式 |
|------|---------|---------|
| 易用性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 灵活性 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 实时预览 | ✅ | ❌ |
| 批量修改 | ❌ | ✅ |
| 适合人群 | 所有用户 | 高级用户 |
| 错误风险 | 低 | 中 |

---

## 🎯 使用建议

### 日常使用
- ✅ 使用预览模式
- ✅ 点击字段直接编辑
- ✅ 简单快捷

### 高级操作
- ✅ 使用源码模式
- ✅ 批量修改多个字段
- ✅ 复制粘贴数据

### 最佳实践
1. **小改动** → 预览模式
2. **大改动** → 源码模式
3. **不确定** → 预览模式（更安全）

---

## ⚠️ 注意事项

### 预览模式
- ✅ 修改会立即生效
- ✅ 自动保存到版本历史
- ⚠️ 暂时只支持基本信息编辑

### 源码模式
- ⚠️ JSON 格式必须正确
- ⚠️ 格式错误会提示
- ⚠️ 不会自动格式化

---

## 🚀 即将支持

### Phase 1（当前）
- ✅ 基本信息编辑
- ✅ 预览/源码切换
- ✅ 实时保存

### Phase 2（计划中）
- ⏳ 技能标签编辑
- ⏳ 工作经历编辑
- ⏳ 项目经验编辑
- ⏳ 教育背景编辑

### Phase 3（未来）
- ⏳ 拖拽排序
- ⏳ 富文本编辑
- ⏳ 图片上传
- ⏳ 模板切换

---

## 🎉 立即测试

1. **刷新页面**（Ctrl+Shift+R）
2. **生成简历**
3. **点击姓名** → 应该能编辑了！
4. **切换到源码模式** → 查看 JSON
5. **修改并保存** → 查看效果

**现在就试试吧！** ✨
