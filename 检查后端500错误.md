# 🔍 后端 500 错误排查

## 📊 当前状态

### ✅ 前端正常
```
✅ 收到更新指令: Array(5)
✅ 当前简历数据: Proxy(Object)
✅ 应用指令后的数据: {basicInfo: {...}, skills: [...], ...}
✅ 最终简历数据: Proxy(Object)
✅ 使用模拟数据进行测试
```

### ❌ 后端错误
```
POST http://localhost:8000/api/v1/generate-resume-chat/ 500 (Internal Server Error)
```

---

## 🔧 排查步骤

### Step 1: 查看后端控制台

在后端控制台应该能看到详细的错误信息，类似：

```python
Traceback (most recent call last):
  File "...", line X, in ...
    ...
ImportError: cannot import name 'generate_resume_chat_response' from 'interviews.ai_services'
```

或

```python
TypeError: ...
AttributeError: ...
ModuleNotFoundError: ...
```

---

## 🎯 可能的原因

### 原因 1: 后端未重启
**症状：** 新代码没有加载

**解决：**
```bash
# 在后端控制台
Ctrl+C  # 停止服务
python manage.py runserver  # 重新启动
```

### 原因 2: 导入错误
**症状：** `ModuleNotFoundError` 或 `ImportError`

**检查：**
```bash
cd G:\documents\GitHub\AI_interview\ai_interview_backend

# 测试导入
python -c "from interviews.resume_chat_view import GenerateResumeChatView; print('✅ 视图导入成功')"
python -c "from interviews.ai_services import generate_resume_chat_response; print('✅ 服务导入成功')"
```

### 原因 3: Python 语法错误
**症状：** `SyntaxError` 或 `IndentationError`

**检查：**
```bash
# 检查文件语法
python -m py_compile interviews/resume_chat_view.py
python -m py_compile interviews/ai_services.py
```

### 原因 4: AI 配置问题
**症状：** `KeyError` 或 `AttributeError`

**可能原因：**
- 用户没有配置 AI 模型
- API Key 不存在
- 模型配置错误

---

## 🛠️ 快速修复

### 方案 1: 添加详细日志

修改 `interviews/resume_chat_view.py`：

```python
def post(self, request, *args, **kwargs):
    try:
        # 添加详细日志
        logger.info(f"收到请求，用户: {request.user.username}")
        logger.info(f"请求数据: {request.data}")
        
        user_message = request.data.get('user_message', '')
        current_resume = request.data.get('current_resume', {})
        chat_history = request.data.get('chat_history', [])
        last_edited_field = request.data.get('last_edited_field')

        if not user_message:
            return Response({
                'error': '用户消息不能为空'
            }, status=status.HTTP_400_BAD_REQUEST)

        logger.info(f"调用 AI 服务...")
        response_data = generate_resume_chat_response(
            user_message=user_message,
            current_resume=current_resume,
            chat_history=chat_history,
            last_edited_field=last_edited_field,
            user=request.user
        )
        
        logger.info(f"AI 响应成功")
        return Response(response_data, status=status.HTTP_200_OK)

    except Exception as e:
        logger.exception(f"详细错误: {str(e)}")
        import traceback
        traceback.print_exc()  # 打印完整堆栈
        
        return Response({
            'error': f'服务器内部错误: {str(e)}',
            'instructions': [],
            'message': '抱歉，AI 服务暂时不可用'
        }, status=status.HTTP_500_INTERNAL_SERVER_ERROR)
```

### 方案 2: 临时禁用真实 AI 调用

修改 `interviews/ai_services.py` 中的 `generate_resume_chat_response`：

```python
def generate_resume_chat_response(...):
    # 临时返回模拟数据，跳过 AI 调用
    return {
        "instructions": [
            {
                "action": "update",
                "path": "basicInfo",
                "value": {
                    "name": "测试用户",
                    "phone": "13800138000",
                    "email": "test@example.com"
                },
                "reason": "初始化基本信息"
            }
        ],
        "message": "✅ 后端接口正常（使用模拟数据）"
    }
```

---

## 📝 调试命令

### 1. 检查文件是否存在
```bash
dir G:\documents\GitHub\AI_interview\ai_interview_backend\interviews\resume_chat_view.py
dir G:\documents\GitHub\AI_interview\ai_interview_backend\interviews\ai_services.py
```

### 2. 检查函数是否存在
```bash
findstr "generate_resume_chat_response" G:\documents\GitHub\AI_interview\ai_interview_backend\interviews\ai_services.py
```

### 3. 测试导入
```bash
cd G:\documents\GitHub\AI_interview\ai_interview_backend
python -c "from interviews.resume_chat_view import GenerateResumeChatView"
```

### 4. 查看完整错误
在后端控制台查看完整的错误堆栈

---

## 🎯 预期的后端日志

### 正常情况
```
[28/Nov/2025 23:XX:XX] "POST /api/v1/generate-resume-chat/ HTTP/1.1" 200 XXX
用户 username 发起简历对话: 帮我生成...
调用 AI 服务...
AI 响应成功
```

### 错误情况
```
[28/Nov/2025 23:XX:XX] "POST /api/v1/generate-resume-chat/ HTTP/1.1" 500 XXX
Traceback (most recent call last):
  File "...", line X, in ...
    [错误详情]
```

---

## 🚀 下一步

### 如果是导入错误
1. 检查文件路径
2. 检查函数名拼写
3. 重启后端服务

### 如果是 AI 配置错误
1. 检查用户是否配置了 AI 模型
2. 检查 API Key 是否有效
3. 使用模拟数据临时替代

### 如果是其他错误
1. 查看完整错误堆栈
2. 根据错误信息修复
3. 添加更多日志

---

## 📞 需要提供的信息

请提供后端控制台的完整错误信息，包括：

1. **错误类型**（ImportError, TypeError, etc.）
2. **错误消息**
3. **完整堆栈跟踪**
4. **发生错误的文件和行号**

---

## ✅ 临时解决方案

**前端已经可以正常工作**（使用模拟数据），所以：

1. ✅ 可以继续测试前端功能
2. ✅ 可以继续开发 UI
3. ⏳ 后端接口可以稍后修复

**当前状态：** 前端完全正常，后端需要修复 ✨
