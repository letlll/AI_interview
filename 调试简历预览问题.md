# 🔍 调试简历预览问题

## 📊 问题现象

**症状：** 右侧简历预览区显示空白（蓝色背景）

**可能原因：**
1. `resumeData` 为 `null` 或 `undefined`
2. `applyInstructions` 函数返回空对象
3. 数据结构不匹配
4. 组件渲染条件不满足

---

## 🧪 调试步骤

### Step 1: 打开浏览器控制台

1. 访问：`http://localhost:5173/dashboard/generate-resume`
2. 按 `F12` 打开开发者工具
3. 切换到 `Console` 标签

### Step 2: 发送测试消息

在输入框输入：
```
帮我生成一份前端工程师的简历
```

### Step 3: 查看控制台日志

**应该看到的日志：**

```javascript
优化后的 Prompt: ...
Token 估算: XXX
AI API 调用失败: AxiosError...
使用模拟数据进行测试
收到更新指令: Array(5)
  0: {action: 'update', path: 'basicInfo', value: {...}, reason: '初始化基本信息'}
  1: {action: 'update', path: 'skills', value: Array(7), reason: '添加前端技能'}
  2: {action: 'add', path: 'workExperience', value: {...}, reason: '添加工作经历'}
  3: {action: 'add', path: 'projects', value: {...}, reason: '添加项目经验'}
  4: {action: 'add', path: 'education', value: {...}, reason: '添加教育背景'}
当前简历数据: null
应用指令后的数据: {basicInfo: {...}, skills: Array(7), workExperience: Array(1), ...}
最终简历数据: {basicInfo: {...}, skills: Array(7), workExperience: Array(1), ...}
```

### Step 4: 检查数据

**如果看到 "最终简历数据" 有内容，但预览区仍然空白：**

在控制台运行：
```javascript
// 检查 resumeData 的值
document.querySelector('.preview-section').__vnode.component.props.resumeData
```

---

## 🔧 可能的问题和解决方案

### 问题 1: applyInstructions 返回空对象

**检查方法：**
```javascript
// 在控制台查看 "应用指令后的数据" 日志
// 如果是 {} 或 null，说明函数有问题
```

**解决方案：**
检查 `useResumeAI.ts` 中的 `applyInstructions` 函数

### 问题 2: 数据没有触发响应式更新

**检查方法：**
```javascript
// 在 Vue DevTools 中查看
// Components → ResumeGeneratorNew → resumeData
```

**解决方案：**
确保使用 `resumeData.value = newData` 而不是修改属性

### 问题 3: 组件渲染条件不满足

**检查方法：**
查看 `ResumePreviewPanel.vue` 的条件：
```vue
<div v-if="!resumeData" class="empty-state">
  <!-- 空状态 -->
</div>

<div v-else-if="editMode === 'preview'" class="resume-render">
  <!-- 简历内容 -->
</div>
```

**解决方案：**
确保：
1. `resumeData` 不为 `null`
2. `editMode` 为 `'preview'`

---

## 🎯 快速测试代码

在浏览器控制台运行以下代码：

```javascript
// 1. 查看当前 resumeData
console.log('resumeData:', window.$vm?.resumeData);

// 2. 手动设置测试数据
const testData = {
  basicInfo: {
    name: '测试用户',
    phone: '13800138000',
    email: 'test@example.com',
    location: '北京'
  },
  skills: ['Vue.js', 'React', 'TypeScript'],
  workExperience: [{
    company: '测试公司',
    position: '前端工程师',
    duration: '2021.01 - 至今',
    description: ['负责前端开发']
  }],
  projects: [],
  education: []
};

// 3. 如果可以访问组件实例，直接设置
// (需要 Vue DevTools)
```

---

## 📝 调试清单

### 数据流检查

- [ ] AI 返回了 instructions
- [ ] instructions 包含 5 个更新指令
- [ ] applyInstructions 被调用
- [ ] applyInstructions 返回了有效数据
- [ ] resumeData.value 被更新
- [ ] 组件接收到新的 props

### 组件检查

- [ ] ResumePreviewPanel 接收到 resumeData prop
- [ ] resumeData 不为 null
- [ ] editMode 为 'preview'
- [ ] 渲染条件满足

### 样式检查

- [ ] preview-section 可见（v-show）
- [ ] resume-render 元素存在
- [ ] 没有 CSS 隐藏元素

---

## 🛠️ 临时修复方案

如果问题仍然存在，可以在 `ResumeGeneratorNew.vue` 中添加初始数据：

```typescript
// 在 script setup 中添加
const resumeData = ref<ResumeData | null>({
  basicInfo: {
    name: '请输入姓名',
    phone: '',
    email: '',
    location: ''
  },
  skills: [],
  workExperience: [],
  projects: [],
  education: []
});
```

这样预览区至少会显示一个空的简历框架。

---

## 🎉 成功标志

当问题解决后，你应该看到：

**控制台：**
```
收到更新指令: Array(5)
当前简历数据: null
应用指令后的数据: {basicInfo: {...}, skills: [...], ...}
最终简历数据: {basicInfo: {...}, skills: [...], ...}
```

**预览区：**
```
姓名：张三
电话：13800138000
邮箱：zhangsan@example.com

专业技能
Vue.js  React  TypeScript  JavaScript  HTML/CSS  Node.js  Git

工作经历
前端工程师 - 某科技有限公司
2021.06 - 至今
• 负责公司核心产品的前端开发工作...
```

---

## 📞 需要帮助？

如果以上步骤都尝试了还是不行，请提供：

1. **完整的控制台日志**（从发送消息到结束）
2. **"最终简历数据" 的内容**（展开查看）
3. **Vue DevTools 中的 resumeData 值**
4. **是否有任何错误信息**

---

## 🚀 立即开始调试

运行测试脚本：
```bash
测试前端简历生成.bat
```

或手动执行：
1. 打开 http://localhost:5173/dashboard/generate-resume
2. 按 F12 打开控制台
3. 输入消息并发送
4. 查看日志输出
